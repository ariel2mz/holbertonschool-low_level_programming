#include "main.h"
/* more headers goes there */

/**
 * _abs- Entry point
 *
 * Description: 'Escribe un texto'
 * @i: caracter a revisar
 * Return: int
 */
int _abs(int i)
{
return (i < 0 ? -i : i);
}
