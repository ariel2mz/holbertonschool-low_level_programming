#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: 'Escribe un texto'
 * Return: Always 0 (Success)
 */
int main(void)
{
/* imprimir un string con puts*/
printf("with proper grammar, but the outcome is a piece of art,\n");
/*retorna 0 porque no importa que retornar*/
return (0);
}
